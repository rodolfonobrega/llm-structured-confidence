[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "llm-structured-confidence"
version = "0.1.1"
description = "Extract per-field confidence scores from LLM structured JSON outputs using token-level log-probabilities."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "llm-structured-confidence contributors" },
]
keywords = ["llm", "logprobs", "confidence", "structured-output", "classification", "enum"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "lark>=1.1",
]

[project.optional-dependencies]
dev = [
    "build>=1.2",
    "pandas>=2",
    "pydantic>=2",
    "pytest>=7.0",
    "python-dotenv",
    "twine>=6",
]
litellm = [
    "litellm",
]
genai = [
    "google-genai",
]
pandas = [
    "pandas>=2",
]

[project.urls]
Homepage = "https://github.com/rodolfonobrega/llm-structured-confidence"
Repository = "https://github.com/rodolfonobrega/llm-structured-confidence"
Issues = "https://github.com/rodolfonobrega/llm-structured-confidence/issues"

[tool.pytest.ini_options]
testpaths = ["llm_structured_confidence/tests"]

[tool.hatch.build.targets.wheel]
packages = ["llm_structured_confidence"]
